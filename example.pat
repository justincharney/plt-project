package example


func foo(b bool, i i32) string {
    return "hello";
}

func main() i64 {
    i64 x = 5;
    y := 6;
    if true {
        z := 2;
    } else {
        z := 3;
    }
    while true {
        x += 1;
    }
    foo(true, i32(1));
    return x + y;
}
